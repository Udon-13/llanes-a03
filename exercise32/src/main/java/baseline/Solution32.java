package baseline;

public class Solution32 {
    /*
    method: numberGuessGame
        do loop while answer != N or n
            prompt for difficult
            rand func set to switch case for diff
            prompt for first answer
            do loop while guess != randNum
                if statement for >
                    too high!
                else
                    too low!
            prompt for playAgain
     */
}
